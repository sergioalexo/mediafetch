name: Release

# Build the Windows installer and macOS bundle and publish them as a
# GitHub release whenever a version tag (v0.2.0, v1.0.0, ...) is pushed.
on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: windows-latest
            args: ""
          - platform: macos-latest
            # Universal binary: runs natively on Apple Silicon and Intel.
            args: "--target universal-apple-darwin"
    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - run: npm ci

      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Signs the updater artifacts so installed apps can self-update.
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ""
        with:
          tagName: ${{ github.ref_name }}
          releaseName: "MediaFetch ${{ github.ref_name }}"
          releaseBody: |
            See the assets below to download and install this version.

            **Windows**: download `MediaFetch_x.y.z_x64-setup.exe` and run it. If SmartScreen shows "Windows protected your PC", click **More info → Run anyway**.

            **macOS**: download the `.dmg` and drag MediaFetch to Applications. Because the app is not yet notarized by Apple, the first launch is blocked: open **System Settings → Privacy & Security**, scroll down and click **"Open Anyway"** next to the MediaFetch message (on macOS Sonoma and older you can instead right-click the app → **Open** → **Open**). This is only needed once.
          releaseDraft: false
          prerelease: false
          args: ${{ matrix.args }}
